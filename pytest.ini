[pytest]
# Look for labs under labs/. Each lab's test file inserts its own folder onto
# sys.path (see the import shim at the top of every test_*.py), and every lab
# uses a unique module name, so single-lab runs and full-suite runs both stay clean.
testpaths = labs
python_files = test_*.py
addopts = -q
